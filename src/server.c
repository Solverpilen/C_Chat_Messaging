#include <stdio.h>
#include <string.h>
#include <sys/types.h>
#include <sys/socket.h>
#include <netdb.h>
#include <arpa/inet.h>
#include <netinet/in.h>
#include "connection.h"
#include "machine.h"


struct void * server_select_machine(char *user_name)
{
    
}

struct machine *machine_init(

    machine *self, 
    char *user_name, 
    struct addrinfo *address_info,
    struct connection *connection
)
{
    
}
